"""speculative decoding metrics

Five additive nullable columns on `run_summary`. Nothing is rewritten.

`vllm bench serve --save-result` reports six speculative-decoding fields when the engine is
speculating. All six were already captured — `run.raw_result` holds the payload and
`run_summary.extra` holds the unmapped keys — but none was a column, so the acceptance rate
could not be charted, compared or filtered on. For a speculative configuration that number
is not another metric; it is the one that explains the others (issue #83).

The sixth field, `spec_decode_per_position_acceptance_rates`, stays in `extra`. It is an
array as long as the speculation depth, so it is neither a scalar nor comparable between
configs that draft to different depths.

NULL here means the engine was not speculating — vLLM emits none of these when it is off.
That is not the same claim as an acceptance rate of zero, which would mean it drafted and
had everything rejected. Every existing row is correctly NULL: those runs were not
speculating.

Revision ID: 09f50f232684
Revises: c2e221f68704
Create Date: (generated)
"""

from __future__ import annotations

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

revision: str = "09f50f232684"
down_revision: str | None = "c2e221f68704"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column("run_summary", sa.Column("spec_acceptance_rate", sa.Float(), nullable=True))
    op.add_column("run_summary", sa.Column("spec_acceptance_length", sa.Float(), nullable=True))
    op.add_column("run_summary", sa.Column("spec_num_drafts", sa.Integer(), nullable=True))
    op.add_column("run_summary", sa.Column("spec_draft_tokens", sa.Integer(), nullable=True))
    op.add_column("run_summary", sa.Column("spec_accepted_tokens", sa.Integer(), nullable=True))
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column("run_summary", "spec_accepted_tokens")
    op.drop_column("run_summary", "spec_draft_tokens")
    op.drop_column("run_summary", "spec_num_drafts")
    op.drop_column("run_summary", "spec_acceptance_length")
    op.drop_column("run_summary", "spec_acceptance_rate")
    # ### end Alembic commands ###
